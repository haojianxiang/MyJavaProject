package haojianxiang.lsp;

public class Test {
	
	/**
	 * Liskov Substitution principle
	 * 里氏替换原则
	 * 
	 * 派生类（子类）对象能够替换其基类（超类）对象被使用
	 * 
	 * 在软件中，把父类都替换成他的子类，程序的行为没有变化，子类必须能替换掉他们的父类型
	 * 
	 */
}
